# E13.2 — Plan adaptatif par discipline (gaps A + B) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fermer les 2 gaps restants d'E13.2 : (A) utiliser le niveau de discipline **effectif** (reconcilié 90 j) dans les régénérations de séance, et (B) différencier la progression par discipline (biais progressif vers les disciplines faibles + garde-fou de hausse hebdo par sport).

**Architecture:** Worker Python uniquement. Un helper partagé `load_effective_strengths` (DRY entre planner et sessions). Deux fonctions pures pour la progression (`distribute_weekly_tss_by_sport(progress=…)` et `cap_weekly_ramp_by_sport(…)`), câblées dans `generate_plan` qui porte l'état « TSS par sport de la semaine précédente ».

**Tech Stack:** Python 3.12, pytest, `unittest.mock.MagicMock` pour la DB Supabase. Modules `worker/src/garmin_sync/coach/`.

## Global Constraints

- **Worker only** : aucun changement frontend ni schéma DB.
- Niveau effectif borné à **±1** du déclaré (déjà garanti par `compute_discipline_levels`).
- Garde-fou : **uniquement les hausses** sont bridées ; deload/taper (baisses) jamais bridés.
- Caps de hausse hebdo par sport : **course 1.10, natation 1.15, vélo 1.20**.
- `distribute_weekly_tss_by_sport` reste **normalisée** (somme = `weekly_tss`) et **rétro-compatible** (`progress` défaut `1.0` = comportement statique actuel).
- Les **37 tests** existants (`test_discipline_level.py`, `test_planner.py`) restent verts.
- Tests : `cd worker && uv run pytest`. Lint/types : `uv run ruff check . && uv run mypy src/`.
- Conventional Commits stricts ; corps ≤ 100 chars/ligne.

---

### Task 1: Helper partagé `load_effective_strengths` (DRY)

**Files:**
- Modify: `worker/src/garmin_sync/coach/discipline_level.py` (ajout d'une fonction)
- Modify: `worker/src/garmin_sync/coach/planner.py:595-598` (utiliser le helper)
- Test: `worker/tests/coach/test_discipline_level.py`

**Interfaces:**
- Consumes: `compute_discipline_levels` (déjà présent dans le même module).
- Produces:
  `load_effective_strengths(db: Any, user_id: str, declared: dict[str, int], today: date | None = None, activities: list[dict[str, Any]] | None = None) -> dict[str, int]`
  — si `activities is None`, charge les activités 90 j de l'utilisateur (`start_time, sport, duration_s, tss`) ; renvoie `compute_discipline_levels(declared, activities, today).effective_strengths`.

- [ ] **Step 1: Write the failing test**

```python
# worker/tests/coach/test_discipline_level.py (ajouter en bas)
from datetime import date
from unittest.mock import MagicMock
from garmin_sync.coach.discipline_level import load_effective_strengths


def test_load_effective_strengths_uses_provided_activities_without_db() -> None:
    # 6+ activités vélo régulières et soutenues sur 90j -> bike déclaré 2 remonte à 3.
    today = date(2026, 6, 28)
    acts = [
        {"start_time": f"2026-0{m}-15T07:00:00Z", "sport": "cycling", "duration_s": 5400, "tss": 80}
        for m in (4, 5, 6)
    ] + [
        {"start_time": f"2026-0{m}-25T07:00:00Z", "sport": "cycling", "duration_s": 5400, "tss": 80}
        for m in (4, 5, 6)
    ]
    db = MagicMock()
    out = load_effective_strengths(
        db, "u1", {"swim": 3, "bike": 2, "run": 3}, today=today, activities=acts
    )
    assert out["bike"] == 3  # remonté de +1
    db.table.assert_not_called()  # activities fournies -> pas de requête


def test_load_effective_strengths_loads_activities_from_db_when_absent() -> None:
    today = date(2026, 6, 28)
    db = MagicMock()
    chain = db.table.return_value.select.return_value.eq.return_value.gte.return_value.execute.return_value
    chain.data = []  # aucune activité -> niveaux = déclarés (safe noop)
    out = load_effective_strengths(db, "u1", {"swim": 2, "bike": 4, "run": 3}, today=today)
    assert out == {"swim": 2, "bike": 4, "run": 3}
    db.table.assert_called_with("activities")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd worker && uv run pytest tests/coach/test_discipline_level.py -k load_effective -q`
Expected: FAIL — `ImportError: cannot import name 'load_effective_strengths'`.

- [ ] **Step 3: Write minimal implementation**

```python
# worker/src/garmin_sync/coach/discipline_level.py (ajouter ; date déjà importé)
def load_effective_strengths(
    db: Any,
    user_id: str,
    declared: dict[str, int],
    today: date | None = None,
    activities: list[dict[str, Any]] | None = None,
) -> dict[str, int]:
    """Niveaux effectifs par discipline (déclaré reconcilié avec l'historique 90 j).

    ``activities`` peut être fourni par un appelant qui les a déjà chargées
    (ex. ``generate_plan``) pour éviter une requête redondante ; sinon on charge
    les 90 derniers jours pour ``user_id``.
    """
    today = today or date.today()
    if activities is None:
        start = (today - timedelta(days=WINDOW_DAYS)).isoformat()
        resp = (
            db.table("activities")
            .select("start_time, sport, duration_s, tss")
            .eq("user_id", user_id)
            .gte("start_time", start)
            .execute()
        )
        activities = list(resp.data or [])
    return compute_discipline_levels(declared, activities, today=today).effective_strengths
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd worker && uv run pytest tests/coach/test_discipline_level.py -q`
Expected: PASS (tous, anciens inclus).

- [ ] **Step 5: Refactor `generate_plan` to use the helper (no reload)**

Dans `worker/src/garmin_sync/coach/planner.py`, remplacer (lignes ~595-598) :

```python
    sports_strengths = profile.get("sports_strengths") or {"swim": 3, "bike": 3, "run": 3}
    effective_strengths = compute_discipline_levels(
        sports_strengths, activities, today=today
    ).effective_strengths
```

par :

```python
    sports_strengths = profile.get("sports_strengths") or {"swim": 3, "bike": 3, "run": 3}
    effective_strengths = load_effective_strengths(
        db, user_id, sports_strengths, today=today, activities=activities
    )
```

Mettre à jour l'import en tête de `planner.py` :
`from garmin_sync.coach.discipline_level import compute_discipline_levels`
→ `from garmin_sync.coach.discipline_level import load_effective_strengths`
(et retirer `compute_discipline_levels` s'il n'est plus utilisé ailleurs dans `planner.py` — vérifier via `grep -n compute_discipline_levels worker/src/garmin_sync/coach/planner.py`).

- [ ] **Step 6: Run planner tests + lint/types**

Run: `cd worker && uv run pytest tests/coach/test_planner.py tests/coach/test_discipline_level.py -q && uv run ruff check . && uv run mypy src/`
Expected: PASS, aucune erreur.

- [ ] **Step 7: Commit**

```bash
git add worker/src/garmin_sync/coach/discipline_level.py worker/src/garmin_sync/coach/planner.py worker/tests/coach/test_discipline_level.py
git commit -m "feat(coach): shared load_effective_strengths helper (E13.2)"
```

---

### Task 2: Gap A — niveau effectif dans les régénérations (`sessions.py`)

**Files:**
- Modify: `worker/src/garmin_sync/coach/sessions.py` (`_load_profile_and_race` ou juste après)
- Test: `worker/tests/coach/test_sessions.py`

**Interfaces:**
- Consumes: `load_effective_strengths(db, user_id, declared, today=None, activities=None)` (Task 1).
- Produces: l'`athlete` passé au LLM a son `sports_strengths` = niveaux **effectifs**.

- [ ] **Step 1: Write the failing test**

```python
# worker/tests/coach/test_sessions.py (ajouter)
from datetime import date
from unittest.mock import MagicMock, patch
from garmin_sync.coach.sessions import _inject_effective_strengths


def test_inject_effective_strengths_overwrites_declared_with_effective() -> None:
    # run déclaré 4 mais ~0 activité sur 90j -> descendu à 3.
    db = MagicMock()
    chain = db.table.return_value.select.return_value.eq.return_value.gte.return_value.execute.return_value
    chain.data = []  # pas d'activité
    athlete = {"sports_strengths": {"swim": 2, "bike": 4, "run": 4}, "ftp_watts": 240}
    out = _inject_effective_strengths(db, "u1", athlete, today=date(2026, 6, 28))
    # sans activité -> safe noop : effectif == déclaré (borné), donc inchangé ici
    assert out["sports_strengths"] == {"swim": 2, "bike": 4, "run": 4}


@patch("garmin_sync.coach.sessions.load_effective_strengths")
def test_inject_effective_strengths_uses_reconciled_levels(mock_load) -> None:
    mock_load.return_value = {"swim": 2, "bike": 5, "run": 3}
    db = MagicMock()
    athlete = {"sports_strengths": {"swim": 2, "bike": 4, "run": 3}, "ftp_watts": 240}
    out = _inject_effective_strengths(db, "u1", athlete)
    assert out["sports_strengths"] == {"swim": 2, "bike": 5, "run": 3}
    # le reste du profil est préservé
    assert out["ftp_watts"] == 240
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd worker && uv run pytest tests/coach/test_sessions.py -k inject_effective -q`
Expected: FAIL — `ImportError: cannot import name '_inject_effective_strengths'`.

- [ ] **Step 3: Write minimal implementation**

Ajouter l'import en tête de `worker/src/garmin_sync/coach/sessions.py` :
`from garmin_sync.coach.discipline_level import load_effective_strengths`

Ajouter la fonction (immutabilité : on renvoie une copie, on ne mute pas l'`athlete` d'origine) :

```python
def _inject_effective_strengths(
    db: Any, user_id: str, athlete: dict[str, Any], today: date | None = None
) -> dict[str, Any]:
    """Remplace les niveaux déclarés de l'athlète par les niveaux effectifs (90 j)."""
    declared = athlete.get("sports_strengths") or {"swim": 3, "bike": 3, "run": 3}
    effective = load_effective_strengths(db, user_id, declared, today=today)
    return {**athlete, "sports_strengths": effective}
```

Brancher l'appel : dans `_load_profile_and_race`, juste avant `return profile, race, weeks`, remplacer `profile` par sa version enrichie. Plus simple et localisé : faire l'injection dans `_load_profile_and_race` en y ayant accès à `db` et `user_id` (déjà ses paramètres) :

```python
    # avant: profile = cast("dict[str, Any]", profile_resp.data or {})
    profile = cast("dict[str, Any]", profile_resp.data or {})
    profile = _inject_effective_strengths(db, user_id, profile)
```

(Insérer cette 2ᵉ ligne juste après le `cast` du profil, avant la requête `race_goals`.)

- [ ] **Step 4: Run test to verify it passes**

Run: `cd worker && uv run pytest tests/coach/test_sessions.py -q`
Expected: PASS (anciens tests sessions inclus).

- [ ] **Step 5: Lint/types**

Run: `cd worker && uv run ruff check . && uv run mypy src/`
Expected: aucune erreur.

- [ ] **Step 6: Commit**

```bash
git add worker/src/garmin_sync/coach/sessions.py worker/tests/coach/test_sessions.py
git commit -m "feat(coach): use effective discipline level in session regen (E13.2 gap A)"
```

---

### Task 3: Gap B1 — biais progressif dans `distribute_weekly_tss_by_sport`

**Files:**
- Modify: `worker/src/garmin_sync/coach/planner.py:41-58` (signature + corps)
- Test: `worker/tests/coach/test_planner.py`

**Interfaces:**
- Produces: signature étendue
  `distribute_weekly_tss_by_sport(*, weekly_tss: float, sports_in_race: list[str], sports_strengths: dict[str, int], progress: float = 1.0) -> dict[str, float]`
  — `progress ∈ [0,1]` module l'amplitude du biais via `bias_curve(p) = 0.5 + 0.5 * clamp(p, 0, 1)`. `progress=1.0` (défaut) = biais plein = comportement statique actuel.

- [ ] **Step 1: Write the failing test**

```python
# worker/tests/coach/test_planner.py (ajouter)
def test_progress_zero_is_more_balanced_than_progress_one() -> None:
    # swim faible (1), bike fort (5). À progress faible le biais est moindre
    # -> part swim plus proche de l'équilibre ; à progress=1 le biais est plein.
    early = distribute_weekly_tss_by_sport(
        weekly_tss=200, sports_in_race=["swim", "bike"],
        sports_strengths={"swim": 1, "bike": 5}, progress=0.0,
    )
    late = distribute_weekly_tss_by_sport(
        weekly_tss=200, sports_in_race=["swim", "bike"],
        sports_strengths={"swim": 1, "bike": 5}, progress=1.0,
    )
    assert late["swim"] > early["swim"]  # on investit plus sur le faible en fin de build
    assert abs(sum(early.values()) - 200) < 0.01  # toujours normalisé
    assert abs(sum(late.values()) - 200) < 0.01


def test_default_progress_is_backward_compatible() -> None:
    # défaut progress=1.0 -> identique à l'ancien comportement statique.
    out = distribute_weekly_tss_by_sport(
        weekly_tss=300, sports_in_race=["swim", "bike", "run"],
        sports_strengths={"swim": 1, "bike": 5, "run": 3},
    )
    # ancien : poids 1.25 / 0.85 / 1.0 -> somme 3.10 ; swim = 300*1.25/3.10
    assert out["swim"] == round(300 * 1.25 / 3.10, 2)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd worker && uv run pytest tests/coach/test_planner.py -k progress -q`
Expected: FAIL — `TypeError: ... unexpected keyword argument 'progress'`.

- [ ] **Step 3: Write minimal implementation**

Remplacer le corps de `distribute_weekly_tss_by_sport` (lignes ~41-58) par :

```python
def distribute_weekly_tss_by_sport(
    *,
    weekly_tss: float,
    sports_in_race: list[str],
    sports_strengths: dict[str, int],
    progress: float = 1.0,
) -> dict[str, float]:
    """Distribue le TSS hebdo entre sports selon le niveau par discipline.

    Niveau (1-5) module la part : faible (1) ~+25 %, fort (5) ~-15 % à biais plein.
    ``progress`` (0..1) module l'amplitude du biais via ``bias_curve`` : ~moitié du
    biais en début de plan, plein en fin de build. Normalisé (somme = weekly_tss).
    """
    p = min(1.0, max(0.0, progress))
    bias = 0.5 + 0.5 * p
    weights: dict[str, float] = {}
    for s in sports_in_race:
        score = sports_strengths.get(s, 3)
        boost = 0.25 - (score - 1) * 0.10  # niveau 1 -> +0.25, 3 -> 0, 5 -> -0.15
        weights[s] = 1.0 + boost * bias
    total_w = sum(weights.values())
    return {s: round(weekly_tss * w / total_w, 2) for s, w in weights.items()}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd worker && uv run pytest tests/coach/test_planner.py -q`
Expected: PASS — y compris les anciens tests de distribution (à `progress=1.0` le poids `1 + boost = 1.25/1.0/0.85`, identique à l'ancien).

- [ ] **Step 5: Commit**

```bash
git add worker/src/garmin_sync/coach/planner.py worker/tests/coach/test_planner.py
git commit -m "feat(coach): progressive weak-discipline volume bias (E13.2 gap B1)"
```

---

### Task 4: Gap B2 — garde-fou de hausse hebdo par sport (fonction pure)

**Files:**
- Modify: `worker/src/garmin_sync/coach/planner.py` (nouvelle fonction + constante)
- Test: `worker/tests/coach/test_planner.py`

**Interfaces:**
- Produces:
  `WEEKLY_RAMP_CAP: dict[str, float] = {"run": 1.10, "swim": 1.15, "bike": 1.20}`
  `cap_weekly_ramp_by_sport(tss_by_sport: dict[str, float], prev_tss_by_sport: dict[str, float] | None) -> dict[str, float]`
  — borne chaque sport à `prev * cap[sport]` **uniquement à la hausse** ; si `prev` est `None` ou si le sport est absent de `prev`, pas de cap pour ce sport ; l'excédent n'est **pas redistribué**.

- [ ] **Step 1: Write the failing test**

```python
# worker/tests/coach/test_planner.py (ajouter)
from garmin_sync.coach.planner import cap_weekly_ramp_by_sport


def test_cap_limits_run_increase_to_10pct() -> None:
    out = cap_weekly_ramp_by_sport({"run": 150.0}, {"run": 100.0})
    assert out["run"] == 110.0  # +10% max


def test_cap_does_not_limit_decreases() -> None:
    out = cap_weekly_ramp_by_sport({"run": 70.0}, {"run": 100.0})
    assert out["run"] == 70.0  # baisse (deload) non bridée


def test_cap_no_prev_is_passthrough() -> None:
    assert cap_weekly_ramp_by_sport({"run": 200.0}, None) == {"run": 200.0}


def test_cap_missing_sport_in_prev_not_capped() -> None:
    out = cap_weekly_ramp_by_sport({"swim": 80.0, "run": 150.0}, {"run": 100.0})
    assert out["swim"] == 80.0  # pas de précédent swim -> non bridé
    assert out["run"] == 110.0


def test_cap_bike_uses_its_own_cap() -> None:
    out = cap_weekly_ramp_by_sport({"bike": 200.0}, {"bike": 100.0})
    assert out["bike"] == 120.0  # vélo +20%
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd worker && uv run pytest tests/coach/test_planner.py -k cap_ -q`
Expected: FAIL — `ImportError: cannot import name 'cap_weekly_ramp_by_sport'`.

- [ ] **Step 3: Write minimal implementation**

Ajouter dans `worker/src/garmin_sync/coach/planner.py` (près des autres constantes de ramp) :

```python
WEEKLY_RAMP_CAP: dict[str, float] = {"run": 1.10, "swim": 1.15, "bike": 1.20}


def cap_weekly_ramp_by_sport(
    tss_by_sport: dict[str, float],
    prev_tss_by_sport: dict[str, float] | None,
) -> dict[str, float]:
    """Borne la hausse hebdo de TSS par sport (anti-surcharge, surtout course).

    Seules les hausses sont bridées (deload/taper intacts). Sans précédent pour un
    sport, pas de cap. L'excédent n'est pas redistribué (sécurité avant volume).
    """
    if not prev_tss_by_sport:
        return dict(tss_by_sport)
    capped: dict[str, float] = {}
    for sport, tss in tss_by_sport.items():
        prev = prev_tss_by_sport.get(sport)
        if prev is None or prev <= 0:
            capped[sport] = tss
            continue
        ceiling = prev * WEEKLY_RAMP_CAP.get(sport, 1.20)
        capped[sport] = round(min(tss, ceiling), 2)
    return capped
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd worker && uv run pytest tests/coach/test_planner.py -k cap_ -q`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add worker/src/garmin_sync/coach/planner.py worker/tests/coach/test_planner.py
git commit -m "feat(coach): per-sport weekly ramp cap guardrail (E13.2 gap B2)"
```

---

### Task 5: Câbler B1 + B2 dans `generate_plan` / `_build_week_sessions`

**Files:**
- Modify: `worker/src/garmin_sync/coach/planner.py` (`generate_plan` boucle ; `_build_week_sessions` signature)
- Test: `worker/tests/coach/test_planner.py`

**Interfaces:**
- Consumes: `distribute_weekly_tss_by_sport(progress=…)` (Task 3), `cap_weekly_ramp_by_sport(…)` (Task 4).
- Produces:
  `_progress_for_offset(offset: int, phases: list[tuple[int, str]]) -> float`
  — `clamp(offset / max(1, last_build_offset), 0, 1)`, où `last_build_offset` = plus grand `offset` de phase `"build"` (sinon dernier `offset` de la liste).
  `_build_week_sessions(...)` perd son appel interne à `distribute_weekly_tss_by_sport` et **reçoit** `tss_by_sport: dict[str, float]` (déjà distribué + cappé par `generate_plan`).

- [ ] **Step 1: Write the failing test**

```python
# worker/tests/coach/test_planner.py (ajouter)
from garmin_sync.coach.planner import _progress_for_offset


def test_progress_for_offset_ramps_over_build() -> None:
    phases = [(0, "base"), (1, "base"), (2, "build"), (3, "build"), (4, "peak"), (5, "taper")]
    assert _progress_for_offset(0, phases) == 0.0
    assert _progress_for_offset(3, phases) == 1.0  # dernière semaine de build
    assert _progress_for_offset(5, phases) == 1.0  # tenu en taper (clamp)


def test_progress_for_offset_no_build_uses_last_offset() -> None:
    phases = [(0, "base"), (1, "base"), (2, "taper")]
    assert _progress_for_offset(0, phases) == 0.0
    assert _progress_for_offset(2, phases) == 1.0
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd worker && uv run pytest tests/coach/test_planner.py -k progress_for_offset -q`
Expected: FAIL — `ImportError: cannot import name '_progress_for_offset'`.

- [ ] **Step 3: Implement `_progress_for_offset` + thread distribution into `generate_plan`**

Ajouter la fonction (près de `_ramp_rate_for_week`) :

```python
def _progress_for_offset(offset: int, phases: list[tuple[int, str]]) -> float:
    """Avancement 0..1 : 0 au début, 1 à la dernière semaine de build (tenu ensuite)."""
    build_offsets = [o for o, ph in phases if ph == "build"]
    last = max(build_offsets) if build_offsets else (phases[-1][0] if phases else 0)
    return min(1.0, max(0.0, offset / max(1, last)))
```

Dans `_build_week_sessions`, supprimer le bloc de distribution interne :

```python
    tss_by_sport = distribute_weekly_tss_by_sport(
        weekly_tss=weekly_tss, sports_in_race=sports_in_race, sports_strengths=sports_strengths
    )
```

et faire recevoir `tss_by_sport` en paramètre. Conséquences sur la signature (cf. Step 4) :
- **Garder** `sports_strengths` : encore utilisé pour `level = athlete_level(sports_strengths)`
  et `max_level = min((sports_strengths.get(s, 3) for s in sports_in_race), default=3)`.
- **Retirer** `weekly_tss` : après suppression du `distribute`, il n'est plus utilisé dans
  `_build_week_sessions` (vérifier : `grep -n "weekly_tss" worker/src/garmin_sync/coach/planner.py`
  ne doit plus le montrer dans le corps de `_build_week_sessions`). `generate_plan` le garde en
  local pour la distribution (Step 3) mais ne le passe plus à `_build_week_sessions`.

Dans `generate_plan`, modifier la boucle `for offset, phase in phases:` pour calculer la distribution + cap et porter l'état précédent :

```python
    week_start = today - timedelta(days=today.weekday())
    all_sessions: list[dict[str, Any]] = []
    prev_tss_by_sport: dict[str, float] | None = None
    for offset, phase in phases:
        ramp = _ramp_rate_for_week(offset, phase)
        base_weekly = max(
            today_state.ctl * 7, weekly_tss_floor_from_hours(profile.get("hours_per_week"))
        )
        weekly_tss = base_weekly * ramp
        if offset == 0:
            weekly_tss *= first_week_tss_multiplier
        progress = _progress_for_offset(offset, phases)
        tss_by_sport = distribute_weekly_tss_by_sport(
            weekly_tss=weekly_tss,
            sports_in_race=sports_in_race,
            sports_strengths=effective_strengths,
            progress=progress,
        )
        tss_by_sport = cap_weekly_ramp_by_sport(tss_by_sport, prev_tss_by_sport)
        prev_tss_by_sport = tss_by_sport
        is_last = offset == weeks_count - 1
        sessions = _build_week_sessions(
            week_offset=offset,
            phase=phase,
            week_start=week_start + timedelta(weeks=offset),
            sports_in_race=sports_in_race,
            sports_strengths=effective_strengths,
            tss_by_sport=tss_by_sport,
            available_days=available_days,
            hours_per_week=profile.get("hours_per_week"),
            is_last_week=is_last,
            race_date=race_date,
            race_sport=race_sport,
            weekly_elevation_by_sport=weekly_elevation_by_sport,
        )
        all_sessions.extend(sessions)
```

- [ ] **Step 4: Update `_build_week_sessions` signature + internal use**

Dans `_build_week_sessions`, ajouter `tss_by_sport: dict[str, float]` aux paramètres (keyword-only, après `sports_strengths`), **retirer** le paramètre `weekly_tss`, supprimer l'appel interne `distribute_weekly_tss_by_sport`, et conserver l'usage de `tss_by_sport` tel quel dans `_training_day_session(..., tss_by_sport=tss_by_sport, ...)`.

- [ ] **Step 5: Run full planner suite + lint/types**

Run: `cd worker && uv run pytest tests/coach/test_planner.py -q && uv run ruff check . && uv run mypy src/`
Expected: PASS. Si un test existant appelait `_build_week_sessions` directement sans `tss_by_sport`, l'adapter en lui passant un `tss_by_sport` explicite (grep : `grep -n "_build_week_sessions(" worker/tests/coach/test_planner.py`).

- [ ] **Step 6: Add an integration test (run ramp cap end-to-end)**

```python
# worker/tests/coach/test_planner.py (ajouter)
def test_distribute_then_cap_run_never_exceeds_10pct_week_over_week() -> None:
    # simule 2 semaines consécutives : la 2e demande +50% sur run -> bridée à +10%.
    w1 = cap_weekly_ramp_by_sport(
        distribute_weekly_tss_by_sport(
            weekly_tss=200, sports_in_race=["run", "bike"],
            sports_strengths={"run": 1, "bike": 5}, progress=0.5,
        ),
        None,
    )
    w2 = cap_weekly_ramp_by_sport(
        distribute_weekly_tss_by_sport(
            weekly_tss=320, sports_in_race=["run", "bike"],
            sports_strengths={"run": 1, "bike": 5}, progress=1.0,
        ),
        w1,
    )
    assert w2["run"] <= round(w1["run"] * 1.10, 2) + 0.01
```

- [ ] **Step 7: Run full worker suite + lint/types**

Run: `cd worker && uv run pytest -q && uv run ruff check . && uv run mypy src/`
Expected: tout vert (46+ tests).

- [ ] **Step 8: Commit**

```bash
git add worker/src/garmin_sync/coach/planner.py worker/tests/coach/test_planner.py
git commit -m "feat(coach): wire progressive bias + ramp cap into plan gen (E13.2 gap B)"
```

---

## Notes d'exécution

- **Ordre** : Task 1 → 5. Dépendances : 2 dépend de 1 ; 5 dépend de 3 + 4 ; 3 et 4 indépendantes.
- `generate_plan` continue de passer `effective_strengths` à `_build_week_sessions` pour
  `athlete_level`/`max_level`/`pick_session_types_for_phase` ; seul le calcul de `tss_by_sport`
  remonte d'un cran (dans `generate_plan`) pour porter l'état inter-semaines.
- Les **caps** s'appliquent **après** le biais progressif : ordre `distribute(progress)` →
  `cap(prev)`.
- Pas de schéma DB, pas de frontend, pas de worker endpoint nouveau.
