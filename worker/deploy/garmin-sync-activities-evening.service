[Unit]
Description=Garmin Sync — evening activities pull
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/bin/docker exec garmin-sync python -m garmin_sync.cron activities
StandardOutput=journal
StandardError=journal
