[Unit]
Description=Garmin Sync — weekly profile refresh (FTP / VMA / FCmax)
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/bin/docker exec garmin-sync python -m garmin_sync.cron profile
StandardOutput=journal
StandardError=journal
